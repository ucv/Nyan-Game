/*
 * Configuration du serveur
 */
define([
],
function (
) {
	var ServerConfig = function () {
		this.local = false; // Si les requête se font en local ou vers le serveur web.
		this.localhost = 'http://localhost/sokoban/';
		this.webhost = 'http://sokonyan.delfisakura.com/src/game/server/php/';
		this.host = this.local ? this.localhost : this.webhost;
	}


	return new ServerConfig();
});